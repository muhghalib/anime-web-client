import { SchedulePage } from '@app/components/pages/schedule';
import { SCHEDULE_PAGE_METADATA } from '@app/constant/metadata';

export const metadata = SCHEDULE_PAGE_METADATA;

export default SchedulePage;
